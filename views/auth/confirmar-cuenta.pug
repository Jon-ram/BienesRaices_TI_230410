extends ../layout/index

block contenido
    .py-10.bg-black.text-white
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal.text-gray Raíces
        h2.text-center.text-2xl.font-extrabold.text-wine= pagina

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-wine' : 'bg-orange-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje

        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-gray uppercase mt-10 block") Ya puedes Iniciar Sesión
            